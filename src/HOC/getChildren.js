import React, { Component } from 'react'export default function (CustomComponent, Children) {  return class extends Component {    render() {      return <CustomComponent getChildren={this.getChildren} {...this.props}/>    }    getChildren(options){      const items = options.items;      return items.map((item, index) => {        return <Children item={item} index={index} {...options.props} key={index} handle={options.handle}/>      })    };  }}
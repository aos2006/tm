import React, {Component, PropTypes} from 'react';import SectionTitle from '../../components/SectionTitle/SectionTitle';import SectionLink from '../../components/SectionLink/SectionLink';import TableListContainer from '../../containers/TableListContainer'class ClientsIndex extends Component {  static propTypes = {  };    state = {    sectionTitle: 'Список клиентов',    sectionLink: 'Создать клиента',    path: '/create',    filtersList: [      {        type: 'id'      },      {        type: 'name'      },      {        type: 'surname'      },      {        type: 'phone'      },      {        type: 'patronymic'      },    ],    headItems: ['id', 'Имя', 'Фамилия' , 'Телефон', 'Отчество',]  };  render() {    return (      <div className="client-index">        <SectionTitle text={this.state.sectionTitle}/>        <div className="admin-layout__action-btn">          <SectionLink  path={this.state.path} text={this.state.sectionLink} />        </div>        <TableListContainer          page={this.props.params.page}          section="clients"          filtersList={this.state.filtersList}          headItems={this.state.headItems}        />      </div>  )  }  }  export default ClientsIndex
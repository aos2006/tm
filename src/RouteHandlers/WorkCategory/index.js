import React, {Component, PropTypes} from 'react';import SectionTitle from '../../components/SectionTitle/SectionTitle';import SectionLink from '../../components/SectionLink/SectionLink'import TableListContainer from '../../containers/TableListContainer'class WorkCategoryIndex extends Component {  static propTypes = {  };  state = {    sectionTitle: 'Категории работ',    sectionLink: 'Редактировать категории',    path: '/update',    filtersList: [      {        type: 'id'      },      {        type: 'name'      },      {        type: 'comment'      },    ],    headItems: ['id', 'Название', 'Комментарий']  };  render() {    return (      <div className="client-index">        <SectionTitle text={this.state.sectionTitle}/>        <div className="admin-layout__action-btn">          <SectionLink  path={this.state.path} text={this.state.sectionLink} />        </div>        <TableListContainer          page={this.props.params.page}          section="workcategory"          filtersList={this.state.filtersList}          headItems={this.state.headItems}        />      </div>    )  }}export default WorkCategoryIndex
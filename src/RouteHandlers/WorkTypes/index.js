import React, {Component, PropTypes} from 'react';import SectionTitle from '../../components/SectionTitle/SectionTitle';import SectionLink from '../../components/SectionLink/SectionLink';import TableListContainer from '../../containers/TableListContainer'class WorkTypesIndex extends Component {  static propTypes = {  };  state = {    sectionTitle: 'Типы Работ',    sectionLink: 'Создать типы работ',    path: '/create',    filtersList: [      {        type: 'id'      },      {        type: 'category'      },      {        type: 'dimension'      },      {        type: 'name'      },    ],    headItems: ['id', 'Категория', 'Единица Измерения', 'Название']  };  render() {    return (      <div className="client-index">        <SectionTitle text={this.state.sectionTitle}/>        <div className="admin-layout__action-btn">          <SectionLink  path={this.state.path} text={this.state.sectionLink} />        </div>        <TableListContainer          page={this.props.params.page}          section="worktypes"          filtersList={this.state.filtersList}          headItems={this.state.headItems}        />      </div>    )  }}export default WorkTypesIndex
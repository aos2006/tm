import {SORT_TABLE} from '../constants';import store from '../store';const defaultState = {  data: [],  sorted: false};export default (state = defaultState, action) => {  const { type, payload, response } = action;  switch (type) {    case  SORT_TABLE:      const sortedResponseList = store.getState().tableListData.data.estimates.filter((item) => {        if (item[payload.fieldType] === payload.fieldValue) {          return item;        }      });      if (payload.fieldValue !== '') return Object.assign({}, state, {data: sortedResponseList, sorted: true});      return Object.assign({}, state, {data: store.getState().tableListData.data.estimates, sorted: false});  }  return state;}
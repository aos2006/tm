import React, { Component, PropTypes } from 'react';import TableTdItem from './TableTdItem';import getChildren from '../../HOC/getChildren';import TableListActions from '../TableListActions/TableListActions'class ClietnsListRow extends Component {  static propTypes = {    item: PropTypes.object  };    getObjectValues(obj){    const arr = [];        for (let key in obj) {      arr.push(obj[key]);    }        return arr;  }    getActions(){    const actions = [{icon: 'fa fa-eye'}, {icon: 'fa fa-pencil'}, {icon: 'fa fa-trash'}];    return actions.map((item, index) => {      return <TableListActions icon={item.icon} key={index}/>    })  }  render() {    return (      <tr className="table-list__row">        {this.props.getChildren({          items: this.getObjectValues(this.props.item)        })}        <td className="table-list__cell">          {this.getActions()}        </td>      </tr>    )  }}export default getChildren(ClietnsListRow, TableTdItem)
import React, { PropTypes } from 'react'import {Link} from 'react-router'function MenuItem(props) {  const { item } = props;  return (      <li className="navigation__item">        <i className={`navigation__icon icon ${item.icon}`}></i>        <Link to={item.link} className="navigation__link">{item.text}</Link>      </li>  )}MenuItem.propTypes = {  };export default MenuItem